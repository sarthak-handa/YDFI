import React from 'react';
import ReactDOM from 'react-dom/client';
import FurnaceDashboard from './FurnaceDashboard.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FurnaceDashboard />
  </React.StrictMode>
);
